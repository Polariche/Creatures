using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//[ExecuteInEditMode]
public class FourLeggedBodyPlan : BodyPlan {
	/*
	void Start() {
		root = new Bone();
		Bone leg00 = new Bone(new Vector3(0, -4, 1));
		Bone leg01 = new Bone(new Vector3(0, -4, 1));
		Bone neck = new Bone(new Vector3(0,0,1));
		Bone leg10 = new Bone(new Vector3(0, -4, -1));
		Bone leg11 = new Bone(new Vector3(0, -4, -1));
		Bone tail = new Bone();


		root.length = Random.Range(0.5f, 2.0f);
		leg00.length = leg01.length = leg10.length = leg11.length = Random.Range(0.5f, 2.0f);
		neck.length = Random.Range(0.5f, 2.0f);
		tail.length = Random.Range(0.5f, 2.0f);


		//root.addToUpper(leg001);
		root.addToUpper(leg00);
		root.addToUpper(neck);
		root.addToUpper(leg01);
		//root.addToUpper(leg011);
		root.calcPosUpper();

		//neck.addToLower(head);
		//neck.calcPosLower();


		root.addToLower(leg10);
		root.addToLower(tail);
		root.addToLower(leg11);
		root.calcPosLower();


		List<Bone> asdf = new List<Bone>();
		draw();
	}
*/
}
